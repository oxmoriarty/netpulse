# Update landing page badge text

Remove the "· Chain ID 4221" suffix from the badge on the landing page so it reads simply "Live on GenLayer Bradbury Testnet".

## Technical details

- File: `src/routes/index.tsx`
- Change the `<span>` text inside the hero section from `Live on GenLayer Bradbury Testnet · Chain ID 4221` to `Live on GenLayer Bradbury Testnet`.

No other files affected.
